# reanalyze codacy test
#% Rest Connector Descriptor 1.0

apiSpec:
  # ❌ Insecure HTTP — Checkov will flag this
  url: 'http://canvasv3.instructure.com/api/v1/canvas-lms-api.raml'

connectorName: CanvasRestLms

connectorGav:
  groupId: com.mulesoft.connectors
  artifactId: canvas-lms-api
  # ❌ "latest" tag — bad practice
  version: latest

# ❌ Missing colon and wrong indent
baseUri
   value: 'https://canvas.instructure.com/api/v1'
   type: parameter
